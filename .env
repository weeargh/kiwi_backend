# --- POSTGRES DATABASE ---
DB_HOST=postgres               # Use 'postgres' if running inside Docker Compose
DB_PORT=5432
DB_USER=uniqueuser123
DB_PASSWORD=password
DB_NAME=uniquedb456

# Full connection string for backend (used if running backend locally)
DATABASE_URL=postgresql://uniqueuser123:password@postgres:5432/uniquedb456

# --- BACKEND SERVER ---
BACKEND_PORT=3001
NODE_ENV=development            # or 'production'

# --- FRONTEND SERVER ---
FRONTEND_PORT=8080

# --- AUTH0 / AUTHENTICATION ---
AUTH0_DOMAIN=your-auth0-domain
AUTH0_AUDIENCE=your-auth0-audience

# --- CORS / FRONTEND URL ---
FRONTEND_URL=http://localhost:8080

# --- (OPTIONAL) EMAIL / SMTP ---
# EMAIL_HOST=smtp.example.com
# EMAIL_PORT=587
# EMAIL_USER=your-email-user
# EMAIL_PASSWORD=your-email-password
# EMAIL_FROM=your-app@example.com

# --- (OPTIONAL) JWT / SECRETS ---
# JWT_SECRET=your-super-secret-key

# --- (OPTIONAL) LOGGING ---
# LOG_LEVEL=info

# --- (OPTIONAL) OTHER SERVICE ENDPOINTS ---
# API_BASE_URL=http://localhost:3001/api

# --- (OPTIONAL) TESTING ---
# CYPRESS_BASE_URL=http://localhost:8080